"""wecomagent Python SDK.

SDK 入口。为 Agent 生成的 python 脚本提供 :mod:`wecomagent` 命名空间。
当前 PR3：Rust 侧 RPC 只路由 `wecom.exec` 一个方法（见 `python_host/dispatch.rs`），
其它 method 都会返回 `-32601 MethodNotFound`，所以上层 SDK 不再预先打桩暴露
`memory` / `llm` / `session` / `log`，减少误用。

脚本使用方式：

    from wecomagent import _rpc
    result = _rpc.call("wecom.exec", {"argv": ["todo", "list"]})

脚本作者完整写法/RPC 方法清单/运行环境约定见
`docs/python-skill-author-guide.md`（仓库根下）。

后续 PR 落地 `memory.*` / `llm.chat` / `session.*` / `log.emit` 时，再按需
把对应模块加回到本包，但脚本侧仍以"文档 + 裸调 `_rpc.call`"为准。
"""

from . import errors  # noqa: F401
from . import _rpc  # noqa: F401

__all__ = ["errors", "_rpc"]
__version__ = "0.0.1"
